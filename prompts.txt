
---- Use as reminder----
For each exercise,
(1) Follow IWYU pattern but keep it lightweight by including the self-header first approach.
(2) In each exercise, have its README.md follow the same pattern as in exercise #1. That is, sections
    and their ordering should be the same. Only thing that should differ is the content and the amount of context
    you provide. Provide at least 2x the context compared to exercise #1 in the Problem Description and Newbie Guidance
    sections. Make sure you do provide a concrete sample input output example with real numbers.
(3) Provide a concrete sample input output with actual numbers in the `Sample Input/Output` section of README.md.
(4) Leave student kernel unimplemented with a TODO
(5) Comment the reference kernel and the cpu kernel implementation liberally

Before you complete your work
(1) Update the top level README.md with any newly added exercises
(2) Ensure everything builds and runs with `make && bin/run_test.sh --test_ref_kernel_only` flag
(3) Regenerate compile_commands.json